# do-tasks-server

## 目录解析

```
-prisma // 数据库模型
-src
  -auth // 授权接口
  -common // 公共接口
  -helper // 工具
  -pipe // 管道
  -prisma // prisma模型
  -task // 任务接口
  -user // 用户接口
  -process-type // 流程类型接口
  -project // 项目接口
  -team // 团队接口
  -validator // 自定义验证器
  -app.module.ts // 主模块
  -main.ts // 主入口
```